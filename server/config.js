const config = {
    port: 8800,
    hostname: "localhost",
    db: {
        host: "localhost",
        user: "root",
        password: "",
        database: "invoice",
    },
};

export default config;
